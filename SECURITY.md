# Security policy

## 安全边界

MACDancer 的首要不变量是：正常开发、自动化测试和 GUI 刷新绝不能改变真实网络接口、MAC 地址、默认路由或 Wi-Fi 关联状态。

只有受限守护进程可以请求系统级 MAC 写入；GUI、Shared 逻辑和测试都只能通过明确的抽象接口观察状态或使用 fake 实现。一次请求必须经过目标接口识别、安全策略、受控写入、读回确认和状态事件这几个阶段。写入失败、超时或读回不一致时，GUI 必须显示失败或不确定状态，不能把请求已发送当作成功。

## 网络安全策略

- 默认路由所依赖的接口不得被自动修改。
- 活跃 Wi-Fi 接口不得被自动修改。macOS 对已关联 Wi-Fi 修改 MAC 往往需要断开关联；这与“网络不断开”的要求冲突。
- 未知接口、不可用接口、非以太网/802.11 接口和安全信息不足的接口默认拒绝。
- 每个接口都有独立的最近地址历史；生成地址必须避开硬件地址、当前地址和近期地址，避免短时间内复用。
- 默认随机地址必须是 48-bit、单播且本地管理地址（LAA）。用户明确开启兼容模式时，只允许复用目标接口经系统读取并验证为全局单播的硬件 OUI；不得接受任意 OUI，且 GUI 必须提示厂商暴露与冲突风险。

即使一个地址在链路层格式上正确，网络仍可能实施 MAC 白名单、NAC、DHCP 绑定或其他策略。因此 MACDancer 不承诺任意网络都会接受新地址。

## IPC 与输入验证

GUI 和守护进程之间传递的数据视为不可信输入。协议负载必须受版本、大小、字段类型、接口身份和 MAC 格式约束；解码失败、版本不兼容、重复请求或状态不一致必须安全失败。敏感状态不应写入不受控日志；日志中的地址应遵循用户的隐私显示设置。

## 测试约束

`./bin/safe-test` 只运行独立的 `MACDancerCoreTests`。测试源码不得包含会变更网络状态的调用或命令，包括 MAC 写入、Wi-Fi 断开/开关、`networksetup` 或守护进程注册。该门禁是辅助措施；真正的隔离来自测试 target 不链接 App、Daemon 或系统写入适配器。

测试 target 不链接 App 或 daemon 可执行文件。为验证持久化和最终读回判定而编入的少量 daemon 核心代码使用 `MACDANCER_CORE_TESTS` 条件：生产命令执行器不会出现在测试二进制中，setter 必须注入 fake executor/provider。

真实接口试验不属于常规 CI 或开发验证。只有在隔离设备、非关键网络、明确书面授权和可恢复访问路径均具备时，才可以设计人工验收；不得以有线接口结果推断 Wi-Fi 行为。

## 报告安全问题

请不要在公开 issue 中披露可复现的高风险网络破坏、权限绕过或敏感信息泄露细节。请通过项目维护者指定的私密渠道报告，并附上受影响版本、最小复现条件、预期影响和可安全验证的方法。维护者应先确认、评估和修复，再协调披露时间。
